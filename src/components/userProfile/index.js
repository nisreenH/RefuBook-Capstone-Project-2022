import React from 'react';

const UserProfile = () => {
  const settings = {
    dots: true,
    infinite: false,
    speed: 500,
    slidesToShow: 3,
    slidesToScroll: 1,
  };

  return (
    <section>
      <p className="container"> Single Item</p>
      <div></div>
    </section>
  );
};

export default UserProfile;
